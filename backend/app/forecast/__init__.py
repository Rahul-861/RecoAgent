"""
Forward Cash Forecaster (README §41).

Additive feature on top of the reconciliation pipeline: reads
TransactionRow / MatchResult but never modifies reconciliation logic,
schemas, or endpoints. See README §41 for the full specification.
"""
