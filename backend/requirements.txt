# ReconAgent backend — compatible version ranges (inspected against actual imports).
# Exact pins belong in a lock file generated from a proven environment, not here.

fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pandas>=2.0,<3
rapidfuzz>=3.0,<4
sqlalchemy>=2.0,<3
python-dotenv>=1.0,<2
python-multipart>=0.0.9,<1
requests>=2.31,<3
pydantic>=2.0,<3
pytest>=8.0,<9
httpx>=0.27,<1
